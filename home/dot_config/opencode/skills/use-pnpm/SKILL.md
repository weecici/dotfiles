---
name: use-pnpm
description: Use pnpm as the package manager for your project.
---

If you want to install JS/TS dependencies, don't use npm or yarn, use pnpm instead! pnpm is a fast and efficient package manager that saves disk space and speeds up installations. To use pnpm in your project, simply run:
```bash
pnpm <command>
```


