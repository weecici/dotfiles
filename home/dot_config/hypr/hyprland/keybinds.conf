
# ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
# ┃                         Keybinds                            ┃
# ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

source = ~/.config/hypr/hyprland/defaults.conf

# https://wiki.hyprland.org/Configuring/Binds/
#
# ======= Default App Keybinds  =======
bindd = $mainMod, T, Opens your preferred terminal emulator ($terminal), exec, $terminal
bindd = $mainMod, E, Opens your preferred filemanager ($filemanager), exec, $filemanager
bindd = $mainMod, B, Opens your preferred browser ($browser), exec, $browser
bindd = $mainMod, C, Opens your preferred editor ($editor), exec, $editor


# ======= Windows Management  =======
bindd = $mainMod, Q, Closes (not kill) current window, killactive,
bindd = $mainMod, I, Switches current window between floating and tiling mode, togglefloating,
bindd = $mainMod, Y, Pin current window (shows on all workspaces), pin
bindd = $mainMod, U, Toggles current window split mode, togglesplit, # dwindle
bindd = $mainMod, F11, Toggles current window fullscreen, fullscreen

# ======= Caelestia =======
bindd = $mainMod, RETURN, Starts caelestia shell, exec, caelestia shell -d
bindd = $mainMod SHIFT, RETURN, Kill caelestia shell, exec, caelestia shell -k
bindd = $mainMod, F, Runs application launcher, exec, caelestia shell drawers toggle launcher
bindd = $mainMod, R, Opens dashboard, exec, caelestia shell drawers toggle dashboard
bindd = $mainMod, BACKSPACE, Runs session option, exec, caelestia shell drawers toggle session
bindd = $mainMod SHIFT, BACKSPACE, Locks session, exec, caelestia shell lock lock
bindd = $mainMod, V, Opens clipboard, exec, caelestia clipboard

# ======= Screenshot =======
bindd = , Print, Creates a screenshot of an area, exec, $shot_region
bindd = CTRL, Print, Creates a screenshot of the active window, exec, $shot_window
bindd = ALT, Print, Creates a screenshot of the active display, exec, $shot_screen

# ======= Grouping Windows =======

bindd = $mainMod, G, Toggles current window group mode (ungroup all related), togglegroup,
bindd = ALT, TAB, Switches to the next window in the group, changegroupactive, f
bindd = ALT CTRL, TAB, Switches to the previous window in the group, changegroupactive, p
bindd = $mainMod ALT, H, Moves current window into the left group, moveintogroup, l
bindd = $mainMod ALT, L, Moves current window into the right group, moveintogroup, r
bindd = $mainMod ALT, K, Moves current window into the above group, moveintogroup, u
bindd = $mainMod ALT, J, Moves current window into the below group, moveintogroup, d

# ======= Volume Control =======

bindel = , XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +5% && pactl get-sink-volume @DEFAULT_SINK@ #| grep -oP '\d+(?=%)' | awk '{if($1>100) system("pactl set-sink-volume @DEFAULT_SINK@ 100%")}' && pactl get-sink-volume @DEFAULT_SINK@ | grep -oP '\d+(?=%)' | awk '{print $1}' | head -1 > /tmp/$HYPRLAND_INSTANCE_SIGNATURE.wob # Raise Volume
bindel = , XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -5% && pactl get-sink-volume @DEFAULT_SINK@ #| grep -oP '\d+(?=%)' | awk '{print $1}' | head -1 > /tmp/$HYPRLAND_INSTANCE_SIGNATURE.wob # Lower Volume
bindel = , XF86AudioMute, exec, amixer sset Master toggle #| sed -En '/\[on\]/ s/.*\[([0-9]+)%\].*/\1/ p; /\[off\]/ s/.*/0/p' | head -1 > /tmp/$HYPRLAND_INSTANCE_SIGNATURE.wob	#Mutes player audio

# ======= Playback Control =======

bindd = , XF86AudioPlay, Toggles play/pause, exec, playerctl play-pause
bindd = , XF86AudioNext, Next track, exec, playerctl next
bindd = , XF86AudioPrev, Previous track, exec, playerctl previous

# ======= Screen Brightness =======

bindel = , XF86MonBrightnessUp, exec, caelestia shell brightness set +5% 	#Increases brightness 5%
bindel = , XF86MonBrightnessDown, exec, caelestia shell brightness set 5%- 	#Decreases brightness 5%
# bindd = $mainMod, O, Reload/restarts Waybar, exec, killall -SIGUSR2 waybar

# ======= Window Actions =======

bindd = $mainMod CTRL, H, Move active window to the left, movewindow, l
bindd = $mainMod CTRL, L, Move active window to the right, movewindow, r
bindd = $mainMod CTRL, K, Move active window upwards, movewindow, u
bindd = $mainMod CTRL, J, Move active window downwards, movewindow, d

bindd = $mainMod, H, Move focus to the left, movefocus, l
bindd = $mainMod, L,  Move focus to the right, movefocus, r
bindd = $mainMod, K, Move focus upwards, movefocus, u
bindd = $mainMod, J, Move focus downwards, movefocus, d
bindd = $mainMod, N, Move focus to next windows, cyclenext,

# Quick resize window with keyboard
# !!! added $mainMod here because CTRL + SHIFT is used for word selection in various text editors
bindd = $mainMod SHIFT, L, Resize to the right, resizeactive, 15 0
bindd = $mainMod SHIFT, H, Resize to the left, resizeactive, -15 0
bindd = $mainMod SHIFT, K, Resize upwards, resizeactive, 0 -15
bindd = $mainMod SHIFT, J, Resize downwards, resizeactive, 0 15
# Resize window with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:273, resizewindow	#Resize the window towards a direction
bindm = $mainMod, mouse:272, movewindow		#Drag window
## Resizing Windows End #

## Move active window to a workspace with $mainMod + CTRL + [0-9]
bindd = $mainMod SHIFT, 1, Move window and switch to workspace 1, movetoworkspace, 1
bindd = $mainMod SHIFT, 2, Move window and switch to workspace 2, movetoworkspace, 2
bindd = $mainMod SHIFT, 3, Move window and switch to workspace 3, movetoworkspace, 3
bindd = $mainMod SHIFT, 4, Move window and switch to workspace 4, movetoworkspace, 4
bindd = $mainMod SHIFT, 5, Move window and switch to workspace 5, movetoworkspace, 5
bindd = $mainMod SHIFT, 6, Move window and switch to workspace 6, movetoworkspace, 6
bindd = $mainMod SHIFT, 7, Move window and switch to workspace 7, movetoworkspace, 7
bindd = $mainMod SHIFT, 8, Move window and switch to workspace 8, movetoworkspace, 8
bindd = $mainMod SHIFT, 9, Move window and switch to workspace 9, movetoworkspace, 9
bindd = $mainMod SHIFT, 0, Move window and switch to workspace 10, movetoworkspace, 10
bindd = $mainMod SHIFT, PERIOD, Move window and switch to the previous workspace, movetoworkspace, +1
bindd = $mainMod SHIFT, COMMA, Move window and switch to the next workspace, movetoworkspace, -1
## Same as above, but doesn't switch to the workspace
bindd = $mainMod CTRL, 1, Move window silently to workspace 1, movetoworkspacesilent, 1
bindd = $mainMod CTRL, 2, Move window silently to workspace 2, movetoworkspacesilent, 2
bindd = $mainMod CTRL, 3, Move window silently to workspace 3, movetoworkspacesilent, 3
bindd = $mainMod CTRL, 4, Move window silently to workspace 4, movetoworkspacesilent, 4
bindd = $mainMod CTRL, 5, Move window silently to workspace 5, movetoworkspacesilent, 5
bindd = $mainMod CTRL, 6, Move window silently to workspace 6, movetoworkspacesilent, 6
bindd = $mainMod CTRL, 7, Move window silently to workspace 7, movetoworkspacesilent, 7
bindd = $mainMod CTRL, 8, Move window silently to workspace 8, movetoworkspacesilent, 8
bindd = $mainMod CTRL, 9, Move window silently to workspace 9, movetoworkspacesilent, 9
bindd = $mainMod CTRL, 0, Move window silently to workspace 10, movetoworkspacesilent, 10
# Window actions End #

# ======= Workspace Actions =======

# Switch workspaces with mainMod + [0-9]
bindd = $mainMod, 1, Switch to workspace 1, workspace, 1
bindd = $mainMod, 2, Switch to workspace 2, workspace, 2
bindd = $mainMod, 3, Switch to workspace 3, workspace, 3
bindd = $mainMod, 4, Switch to workspace 4, workspace, 4
bindd = $mainMod, 5, Switch to workspace 5, workspace, 5
bindd = $mainMod, 6, Switch to workspace 6, workspace, 6
bindd = $mainMod, 7, Switch to workspace 7, workspace, 7
bindd = $mainMod, 8, Switch to workspace 8, workspace, 8
bindd = $mainMod, 9, Switch to workspace 9, workspace, 9
bindd = $mainMod, 0, Switch to workspace 10, workspace, 10
# Scroll through existing workspaces with mainMod + "," or "."
bindd = $mainMod, PERIOD, Scroll through workspaces incrementally, workspace, e+1
bindd = $mainMod, COMMA, Scroll through workspaces decrementally, workspace, e-1

# Special workspaces (scratchpads)
bindd = $mainMod, minus, Move active window to Special workspace, movetoworkspace, special
bindd = $mainMod, equal, Toggles the Special workspace, togglespecialworkspace, special
bindd = $mainMod, F1, Call special workspace scratchpad, togglespecialworkspace, scratchpad
bindd = $mainMod ALT SHIFT, F1, Move active window to special workspace scratchpad, movetoworkspacesilent, special:scratchpad

# ======= Additional Settings =======

# https://wiki.hyprland.org/Configuring/Binds
binds {
  allow_workspace_cycles = 1
  workspace_center_on = 1
	movefocus_cycles_fullscreen = true
	window_direction_monitor_fallback = true
}
